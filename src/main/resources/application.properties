spring.application.name=FoodWasteManagement
server.port=8081

# PostgreSQL Database Configuration (pgAdmin)
spring.datasource.url=jdbc:postgresql://localhost:5433/FoodForward
spring.datasource.username=postgres
spring.datasource.password=Priyanka
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=FeedForwardSecretKey2024FeedForwardSecretKey2024FeedForwardSecretKey2024
jwt.expiration-days=30
